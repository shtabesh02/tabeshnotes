{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "index.html"
    }
  ],
  "builds": [
    {
      "src": "src",
      "command": "npm run build",
      "output": "dist"
    }
  ]
}